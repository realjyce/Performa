bin/
obj/

tools/Shot/*.png

# never commit OAuth client credentials
google-credentials.json
**/google-credentials.json

# Shipped credentials: never in the public repo
app-credentials.json
**/app-credentials.json

# Screenshot harness output
.shots/

# Published build output
dist/

# Installer build output (the .iss script itself is tracked)
installer/out/
